import { EmployeeScheduleDay } from './employee-schedule-day';

describe('EmployeeScheduleDay', () => {
  it('should create an instance', () => {
    expect(new EmployeeScheduleDay()).toBeTruthy();
  });
});
