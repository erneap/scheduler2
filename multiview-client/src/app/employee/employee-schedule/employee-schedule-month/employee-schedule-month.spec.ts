import { EmployeeScheduleMonth } from './employee-schedule-month';

describe('EmployeeScheduleMonth', () => {
  it('should create an instance', () => {
    expect(new EmployeeScheduleMonth()).toBeTruthy();
  });
});
